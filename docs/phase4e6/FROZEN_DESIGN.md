# P4E6 frozen design

This design is subordinate to `artifacts/phase4e6/frozen/authority_contract.json` and is frozen before any P4E6 acquisition. It permits reproducible non-official sources, requires two independently captured accessible sources per issue, records lineage and suspected common upstream, and quarantines disagreement or insufficient consensus.

All operational predictors are shifted by at least one draw. No current draw metadata, current draw imputation, report-label reuse, probability-temperature sharpening, or other artificial widening of probability spread is allowed.

The candidate family, expanding-origin folds, proper scores, paired bootstrap, Holm family, tolerances, coverage requirements, calibration gates, and promotion gates are exactly those in `config/phase4e6_registry.json`. A 120-draw report must be unused by every earlier selection/model fit as well as disjoint from P4E3, P4E4, P4E5 reports and original-200. If the audit cannot prove such a window for both games, P4E6 terminates `PROSPECTIVE_ONLY` and issues a prospective ledger without opening another historical report.
