# R05 agent constraints

- No repository edits are authorized.
- Do not install or download dependencies.
- Run only the fast provider checks in `PROMPT.md`.
- Preserve every `artifacts/` path and every Phase 3 scientific contract.
- A clean existing HEAD is the intended candidate; no empty commit is needed.
