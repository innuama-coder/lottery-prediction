# Claude continuation context

This is a recovered continuation after an executor deadline, not a fresh
implementation. Inspect the existing working tree changes before editing.
Complete and verify them against the R01 contract, then commit all intended
changes. The controller will independently inspect and rerun acceptance.
