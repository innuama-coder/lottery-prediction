# R04 execution context

Follow `PROMPT.md` exactly. This task fixes one synthetic test fixture so the
Phase 3 suite runs in the independent verifier's source-only checkout.

Do not edit production code, plans, contracts, configuration, schemas, or
artifacts. Do not compensate for a missing `.git` directory by creating fake
Git metadata or skipping tests. Commit the single-file fix before finishing.
