# Preserved adverse-evidence lineage

All earlier releases, commits, and controller results remain unchanged:

- r10 macOS local acceptance failed on exact raw-binary64-derived
  `top1000.0.score_identity`; the stable key migration preserved ticket
  membership/order/rank.
- r11's first numeric controller run failed at
  `top1000.866.joint_probability`.
- the full r11 macOS replay preserved 163 legacy bound failures and disproved
  the incomplete 17-ULP diagnosis; commits `18b25e21` and `eb9c124d` remain
  immutable failed-hypothesis evidence.
- the c5a9 preflight preserved 17 remaining failures: one nested
  `number_features` class and 16 propagated `top_zone_rows` score failures.
- the f2f macOS pre-allocation replay passed after only those two source classes
  were isolated. This PASS authorizes r12 allocation but is not final macOS r12
  acceptance.

r11 remains byte-for-byte unchanged at its recorded 178-file inventory. No
prior release identity was reused or mutated.
