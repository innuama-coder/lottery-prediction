# TASK-001：官方开奖数据可行性实证

## 研究合同

- 工作强度：`multi_cycle_observation`。
- 方法：falsification-first、point-in-time、power-aware、source-traceable；机器产物先过 Schema，再做语义硬门；两个彩种分别判定，最后只输出一个项目决策。
- 来源策略：优先使用国家发行机构正式规则、开奖公告或结构化入口；省级官方渠道只作核对和补充；第三方只可发现问题，不得补充官方真值。计划来源依次为 `docs/research/lottery-autoresearch-technical-strategy.md`、`docs/roadmap/phase-0-data-feasibility-plan.md`、`docs/roadmap/phase-0-acceptance-contract.json` 和可合规访问的官方页面。
- 证据质量：原始响应字节、批准保留的响应元数据、URL、UTC 抓取时间、SHA-256、解析器哈希、环境锁和规范输出哈希构成最小可采信链；只见搜索摘要、没有原始载荷或需要绕过控制的事实不得进入 `verified`。
- 资源与超时：请求、重试、1 GiB 存储、120 CPU 分钟和 8 小时人工复核预算已在 P0-01 冻结。到 `2026-08-15T17:00:00Z` 仍未完成最少观察周期或关键来源持续不可用时必须 `HOLD`，不得延长截止时间掩盖失败。
- 下游研究报告交接位置为 `docs/research/`；TASK-002 只有在本任务至少一个彩种获得 `PASS_FULL` 或 `PASS_LIMITED` 且 G-HANDOFF 通过后才能开始。

## 1. 当前状态

阶段 0 只回答一件事：能否在不手工改号、不绕过访问控制的前提下，把大乐透和双色球的官方开奖事实做成可追溯、可重放、可修订的数据产品。本阶段不训练模型、不生成预测号码，也不评价盈利能力。

截至 2026-08-01：

| 子阶段 | 状态 | 可验收交付物 |
| --- | --- | --- |
| P0-01A 原始范围冻结 | 已通过并归档 | 批次 A 的 38 个文件及哈希清单位于 `artifacts/phase-0/batches/p0-20260801-a/` |
| P0-01B 机器合同修复层 | 已通过独立复核 | 继承 A 的冻结输入和决策面；迁移门证明没有事后缩小范围、样本、预算或日程 |
| P0-02 来源与访问审计 | 已完成 | 审计门通过；逐彩种运行状态为大乐透 `ready`、双色球 `hold` |
| P0-03 字段与规则边界 | 已通过独立复核 | 两个彩种共 700 条期号映射，时间空值、规则证据和活动状态均可机器验证 |
| P0-04 小样本采集与解析 | 已通过独立复核 | 大乐透真实探针和离线重放通过；双色球在联网前按合规策略 HOLD |
| P0-05 历史覆盖 | 工作包已通过；覆盖门未通过 | 大乐透目标 1/350、最低区间 1/200；双色球均为 0，缺口已逐期分类 |
| P0-06 在线观察与恢复 | 调度已安装并通过完整反查；等待跨周期执行 | 24 个冻结触发器；首个触发为 2026-08-02 22:00（北京时间），截止后禁止联网补跑 |
| P0-07 独立重放与决策 | 未开始 | 依赖 P0-05、P0-06 |

当前 `active_games=[]`、`excluded_games=[]` 只是“尚未形成终局分类”，不是遗漏。最终 `artifacts/phase-0/stage1-handoff-fixture.json` 必须把 dlt、ssq 各自且仅放入一个集合；在 P0-07 前不得提前宣布任何彩种进入下游。

## 验收标准映射

| 验收 ID | 当前状态 | 最终证据 |
| --- | --- | --- |
| `TASK-001-AC-01` | 进行中；14 个硬门尚未全部执行 | `artifacts/phase-0/phase-0-acceptance-report.md`、`artifacts/phase-0/replay-report.json` |
| `TASK-001-AC-02` | 进行中；P0-04 至 P0-07 产物尚未齐全 | `artifacts/phase-0/schemas/`、`artifacts/phase-0/evidence-manifest.jsonl` |
| `TASK-001-AC-03` | 未开始；不得提前签署 | `artifacts/phase-0/reviewer-attestation.json`、`artifacts/phase-0/replay-report.json` |
| `TASK-001-AC-04` | 本报告持续维护，终局前只陈述已验证事实 | `docs/research/TASK-001-official-data-source-feasibility.md` |

独立复核人事前指派记录已在 `artifacts/phase-0/reviewer-assignment.json` 冻结；事后凭证 `artifacts/phase-0/reviewer-attestation.json` 必须等 P0-07 从空派生目录重放后才可生成。目前该文件不存在是正确状态。

## 2. P0-01：冻结事实

- 冻结批次：`P0-01-p0-20260801-a`。
- 两个彩种目标区间：`2024001—2026050`。
- 两个彩种最低可行区间：`2025001—2026050`。
- 历史核对样本：按彩种、自然年分层；固定种子 `20260801`；每层 10 期，每个彩种 30 期。样本在查看历史结果前生成。
- 在线观察：每个彩种 6 个开奖日、主源与可用官方核对源各一次逻辑请求，共 24 个逻辑请求；接受截止为 `2026-08-15T17:00:00Z`。
- 禁止请求头：`Authorization`、`Cookie`、`Proxy-Authorization`；不得绕过登录、验证码、令牌、WAF 或限流。
- P0-07 复核人未参与解析器编写或证据修改。

冻结验证命令当前退出码为 0，当前 Phase 0 回归测试为 76/76 通过。完整重放命令已经冻结，但在后续证据未齐时应当失败闭合，不能用 P0-01 至 P0-04 的局部通过冒充阶段 0 通过。

批次 A 的机器合同后来被发现存在可执行性矛盾，因此该局部通过不能作为 P0-03/P0-04 的通过证据。A 已按原始字节归档，批次 B 直接继承 A 的 `scope-freeze.json`、`observation-plan.json` 和 `reviewer-assignment.json`；迁移清单 `artifacts/phase-0/batch-migration-p0-20260801-b.json` 已证明冻结输入和决策面哈希相同，并明确披露迁移前已见到的来源结果。

## 3. P0-02：官方来源实证

### 3.1 大乐透

权威主源为国家体育总局体育彩票管理中心的[大乐透官方开奖入口](https://m.lottery.gov.cn/mkjdlt/)。本执行环境在 2026-08-01 对国家入口、竞彩网历史页及候选历史接口的公开 GET 均收到 HTTP 567；没有重定向，也没有尝试伪造身份或绕过限制。

可公开访问的官方省级渠道为[广东省体育彩票中心逐期开奖公告](https://www.gdlottery.cn/f_html/kjgg/P085_26070.html)。抽查响应为 HTTP 200、UTF-8 静态 HTML，不需要登录、Cookie、验证码或 JavaScript。页面包含期号、开奖日期、全国销售额、出球顺序、排序后的开奖号码、奖级、中奖注数、奖金、奖池和兑奖截止日。

广东渠道属于官方渠道，但接受国家体彩业务体系指导，不能把它与国家主源的相同号码表述为“独立双源真值”；本项目将其标记为 `shared_upstream`。

工程访问策略只批准广东静态开奖页在冻结预算内每分钟最多 1 次、单次尝试的公开 GET，用于最少必要的内部证据和事实字段抽取。检查时没有发布可用 robots 指令，也没有发现开放许可；这不等于法律授权，不允许批量镜像、公开再发布或商业再利用。

### 3.2 双色球

权威主源为中国福利彩票发行管理中心的[双色球历史开奖入口](https://www.cwl.gov.cn/ygkj/wqkjgg/ssq/)。本执行环境对历史页、逐期页和候选分页接口的公开 GET 收到 HTTP 403；没有绕过。

可公开访问的官方省级渠道为[广东省福利彩票发行中心双色球历史页](https://www.gdfc.org.cn/sjfx/ssq_200.html)。抽查响应为 HTTP 200、GBK、服务端 HTML；页面最多提供最近 200 期核心号码。该页不提供完整开奖时间，也不能单独覆盖冻结目标区间。广东逐期文章明确标注“来源：中国福彩”，同样属于 `shared_upstream`。

该站[服务协议](https://www.gdfc.org.cn/about02.html)对未经授权复制、再造和派生使用有明确限制，robots 在本次检查中不可取得。因此该来源记为 `hold_pending`，不得用于保存完整响应或批量重放；国家主源的 HTTP 403 同样不得绕过。

### 3.3 来源门结论

- `G-AUTHORITY`：当前证据支持通过。两个彩种均识别出法定发行机构的国家主源、至少一个官方省级渠道，并明确了共享上游关系；未使用第三方数据作为真值。
- `G-COMPLIANCE`：来源审计本身通过，因为访问控制、条款、robots、版权、限制和未决问题都已如实记录；通过不代表所有来源都获准采集。逐彩种机器结论为大乐透 `ready`、双色球 `hold`。
- 可采集能力仍有限：两个国家主源对本环境不可用；大乐透只有省级共享上游探针可运行，双色球没有当前获准的采集来源。因此，P0-02 通过不等于 P0-05 覆盖通过。

## 4. P0-03：规则与字段设计

### 4.1 正交规则轴

每一期同时映射四个独立维度：

1. `number_space_version`：号码分区、范围和选取个数。
2. `draw_process_version`：官方规则公开的封存、公证、摇奖及分区先后；不把设备身份或未公告变化猜成新版本。
3. `prize_rule_version`：基础奖级和计奖规则。
4. `active_promotion_ids`：可同时存在多个全国派奖覆盖；不把省级赠票、扫码抽奖等营销活动混入全国计奖规则。

大乐透在冻结区间始终为前区 1—35 选 5、后区 1—12 选 2；第 26014 期从九奖级切换到七奖级。双色球始终为红球 1—33 选 6、蓝球 1—16 选 1；第 2026014 期启用新计奖规则。

规则边界的主要官方证据包括：[大乐透财政部审批](https://www.mof.gov.cn/gp/xxgkml/zhs/202601/t20260116_3982042.htm)、[26013期](https://www.gdlottery.cn/f_html/kjgg/P085_26013.html)、[26014期](https://www.gdlottery.cn/f_html/kjgg/P085_26014.html)、[双色球财政部审批](https://www.mof.gov.cn/gp/xxgkml/zhs/202601/t20260116_3982040.htm)和[双色球新规则 PDF](https://www.gdfc.org.cn/resource/2026/2026011601.pdf)。

### 4.2 派奖覆盖

- 大乐透 2024：高奖级派奖 `24030—24049`；固定奖派奖 `24030—24062`，两者重叠但结束期不同。
- 大乐透 2025：全国派奖 `25038—25057`；25057 仍有派奖字段，25058 已无。
- 大乐透 2026：冻结区间内从 26050 开始固定奖派奖。
- 双色球 2024：一等奖特别派奖 `2024126—2024145`；六等奖翻番 `2024126—2024144`。
- 双色球 2026：从 2026014 起进入“福运奖”特别规定；它是带滞回的状态机，开启门槛和停止门槛不同，不能每期用同一阈值重算。冻结末期 2026050 仍处于开启状态。

当前 `rule-bundles.json` 为两个彩种各生成 350 条唯一映射。2025 年双色球没有发现全国派奖公告；这只表示“审计到的官方目录中未观察到”，不是对不存在的绝对证明。P0-05 将用逐期奖项字段继续核对，若发现相反证据必须追加修订，不得覆盖旧证据。

### 4.3 时间与号码语义

- 排序后的官方开奖号码与物理出球顺序是两个不同事实。阶段 0 的核心号码只采用排序后的官方开奖号码；物理顺序不能混入。
- `page_published_at` 只接受页面明确的发布时间；HTTP `Date`、`Last-Modified`、URL 日期和本系统抓取时间都不能替代。
- `first_seen_at`、`retrieved_at` 和 `available_at` 是系统事实，不能倒推开奖或发布时间。
- normalized-record v1.1 把官方页面可证明的 `draw_date_local` 与精确 `draw_at` 分开：日期必填，实际摇奖时刻没有证据时为 `null`。非 `verified` 记录的 `available_at` 和 `corroboration_tier` 必须为 `null`；`verified` 则必须有可用时刻和非空核对等级。故障注入证明这组约束是双向的。

## 5. P0-04 的最小验收范围

P0-04 不追求一次性抓完整历史。它只需实证以下闭环：

1. 用公开低频 GET 保存准确的响应字节、请求 URL、跳转链、白名单响应头、抓取时间和 SHA-256。
2. 同一原始字节、同一解析器哈希和同一环境锁产生相同规范记录哈希。
3. 号码数量、范围、重复、排序、期号和编码均严格校验。
4. 畸形 HTML、缺字段、越界、重复号、编码损坏、空正文和未验证 OCR 一律失败闭合，不产生 `verified` 记录。
5. 国家主源不可达时，如实记录 `unavailable`；不能由省级核对源静默替代主源角色。
6. 每个获准采集的彩种至少用一个真实官方响应做端到端重放；未获准的彩种必须在创建网络客户端前 HOLD，且不得产生原始或规范记录。

### 5.1 实际探针结果

- 时钟检查：`time.windows.com/w32tm`，偏差 2 秒，未超过冻结的 5 秒上限。
- 大乐透真实探针：第 `2026050` 期，官方页面日期 `2026-05-09`，前区 `06 10 14 23 33`，后区 `08 10`。原始响应 6,774 字节，SHA-256 为 `89cd32a94313086fa9a7d5a039d21566934b23b72d4268fe2eb7b725147718ad`；规范记录哈希为 `28ae23739b8b4b2171188a267d101279c1201ec8df20c4d871d0c2c6cc25c619`。
- 证据状态保持 `unverified`，`draw_at=null`、`available_at=null`、`corroboration_tier=null`；省级共享上游来源没有被冒充为权威主源或独立核验。
- evidence manifest v1.2 分别记录 `request_method=GET`、HTTP 内容解码未发生、字符解码采用 UTF-8、字段解析已执行且成功。`verify-captures` 从同一原始字节在临时目录重放 1 条记录，退出码为 0，`network_used=false`。
- 首版 manifest 的审计字段缺陷已完整归档在 `artifacts/phase-0/batches/p0-04-20260801-a/`。受保护迁移把旧 manifest 哈希 `69dc24ce6aa72c05c5839b7dc1cbf6e83ec5b4ba66742c71a2502b13d8898e64` 升级为 `110f43b4482ddc332b6762795c2ca7f5edb36a2c679a2f1e2d5dfad17bf66877`；时钟、环境锁、原始响应、解析结果和规范记录五项字节均未改变。迁移可幂等重放，篡改旧版或新版都会 HOLD 且不写入。
- 双色球 collect 命令在创建网络客户端前返回合规 HOLD；没有发起请求，也没有生成双色球 raw、parsed 或 normalized 文件。
- 解析器实现由非 Reviewer 的 `/root/phase0_tooling` Agent 完成，独立 Reviewer 从未编写或修改解析器及证据；实际角色隔离成立。冻结的 `reviewer-assignment.json` 中 parser-author 槽位仍写为 unassigned，作者身份没有在编码前登记，也没有被事后改写冻结输入。该登记时序缺口作为治理限制保留到 P0-07，不用追认方式伪造事前记录。

## 6. P0-05：历史工作集与覆盖结论

P0-05 工作集不按已观察到的号码或 HTTP 成功率选取。它是冻结随机样本与规则映射变化点前一期、当期、后一期的机械并集：大乐透为 30 个样本、9 个变化点、52 个唯一期号；双色球为 30 个样本、6 个变化点、45 个唯一期号。已有的大乐透 `2026050` 只改变请求处置为“复用”，没有把它从工作集中删除。

冻结总请求上限为 96，P0-06 预留 24，P0-04 已执行 1 次。P0-02 可证明的直接请求下界为 8，但早期没有完整 request ledger，精确值未知；因此 P0-05 可用额度只能写成 `71 - N_p002`。大乐透剩余 51 个新请求仅在 `N_p002 <= 20` 时才不超预算。由于该前提目前不可审计证明，`certified_authorized_new_requests=0`，本工作包没有联网。

覆盖报告据此给出：

- 大乐透：目标区间观测 1/350，最低区间观测 1/200，`coverage_tier=none`；其余 349 期分类为“权威主源阻断且请求预算未完成对账”。
- 双色球：目标区间观测 0/350，最低区间观测 0/200，`coverage_tier=none`；350 期全部分类为“没有获准来源的合规 HOLD”。
- `reconciliation.jsonl` 保持空文件，因为没有任何权威主源记录可与核对渠道配对；空文件比虚构 primary 或伪造双源一致更可信。

独立 Reviewer 机械重算了两个工作集、预算公式、逐期缺口和六类故障注入。P0-05 工作包结论为 PASS，含义仅是“计划、预算封锁和覆盖缺口诚实可重放”，不表示 G-COVERAGE 通过。

## 7. P0-06：在线观察调度与安装前验收

P0-06 没有修改 P0-01 冻结的 24 个逻辑请求、时间、预算或截止时刻。运行计划位于 `artifacts/phase-0/p0-06-runtime-plan.json`，其 SHA-256 为 `afce77bfc40130dd6ffa9f0a22f3487817557c21f4f262210a6f84951715eede`，独立 sidecar 与文件一致。调度范围为：

- 6 个大乐透广东官方期号页具备精确、逐 request ID 的低频联网资格；
- 6 个大乐透国家主源请求保持 `policy_blocked`；
- 6 个双色球国家主源请求保持 `policy_blocked`，6 个省级核对请求保持 `compliance_hold`；这 18 个请求只写零网络审计记录；
- 命令行 `--allow-network` 不是授权来源。执行还必须同时通过规范字节、Schema、完整 observation/source 输入哈希、计划机械重建、计划 sidecar 和安装时固定的 expected plan SHA；任一不一致都在时钟检查和网络客户端创建前 HOLD；
- 未来观察期号 `2026087—2026093` 超出冻结历史规则映射截止的 `2026050`。系统禁止推断未来活动或规则；若核心字段可解析，只保留 raw 与 `invalid` evidence，明确分类为规则映射尚不可用，不生成 normalized 或 `verified` 记录；
- 网络成功和失败都记录真实开始、完成时间并进入跨进程一分钟限流；验收截止 `2026-08-15T17:00:00Z` 后恢复时，计划联网项只记录 `acceptance_cutoff_passed_no_collection`，不会顺延采集。

安装前经历四轮只读独立审查。首轮发现完成时间、运行前冻结复核、计划任务安全设置反查和运行文件哈希链不完整；第二轮发现 evidence/raw 交叉错引仍可能通过；第三轮发现合法 raw-only evidence 会被最终 provenance 错误拒绝；这些根因均以关系约束、失败注入和跨阶段重放修复。最终 Reviewer 明确给出“PASS，准许安装”，但没有提前生成 P0-07 的 `reviewer-attestation.json`。

最终离线基线为 111/111 测试通过；P0-01、P0-02、P0-03、P0-05、P0-06 可用阶段验证均退出 0，P0-04 的 `verify-captures` 离线重放退出 0。验证冻结清单包含 21 个 Schema 和 9 个验证/运行文件；三个 P0-01 冻结输入与批次 A 字节级一致，决策面哈希仍为 `d7959ea51e11bbaea95e1b5b4912aef310be39b65b1ca573fe3bdb7f092949bf`。

当前用户计划任务 `AutoresearchLotte-P0-06` 已安装。安装后反查显示 `Installed=True`、`MatchesFrozenPlan=True`：动作、参数、工作目录、24 个触发时刻、`StartWhenAvailable`、15 分钟执行上限、`IgnoreNew`、当前用户 SID、Interactive 和 Limited 全部匹配。系统报告下一次运行时间为北京时间 `2026-08-02 22:00:00`，漏跑数为 0，任务尚未运行。`soak-run-log.jsonl` 仍为 0 字节，`evidence-manifest.jsonl` 在安装期间未变化，因此尚未发生 P0-06 网络采集。机器可读时间点快照位于 `artifacts/phase-0/p0-06-scheduler-install-audit.json`：12/12 检查为真、`last_run_state=never_run`、`last_task_result=267011`、`missed_runs=0`，并明确声明它不是对未来 OS 状态的持续证明。

P0-06 目前只能称为“调度准备与安装门通过”，不能称为观察完成。必须执行到冻结截止并对账全部 24 个请求后，才能进入 P0-07。当前 full verifier 因 `revision-report.json` 等终局产物尚未生成而失败，这是跨期开奖观察尚未完成的预期状态，不是阶段 0 已通过。

## 8. 当前不能下的结论

- 不能说完整目标区间已经覆盖。
- 不能说国家主源已具备稳定自动访问能力。
- 不能把共享上游说成独立核验。
- 不能把计划开奖时刻当作逐期实际开奖时刻。
- 不能宣布阶段 0、阶段 1或任何预测模型已经通过。
- 数据可行性通过后，也不能由历史开奖数据推出“中奖概率可无限逼近 100%”；公平独立开奖下，历史模式不会消除组合空间的不确定性。
