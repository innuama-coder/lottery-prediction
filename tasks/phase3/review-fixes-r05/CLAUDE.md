# R05 execution context

This is a recovered-candidate delivery task. The implementation is already in
HEAD. Perform only the fast checks in `PROMPT.md`, make no edits, install no
packages, leave the worktree clean, and exit promptly so independent acceptance
can run the complete regression matrix.
