{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lottery.local/schemas/phase4/backtest.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "artifact_type",
    "game",
    "selected_candidate_identity",
    "selection_indices",
    "report_only_indices",
    "model_selection_receipt_path",
    "model_selection_receipt_sha256",
    "selection_receipt_hash",
    "overlap_count",
    "comparator",
    "report_only_summary",
    "metrics",
    "scientific_status",
    "unfavorable_results_retained",
    "status"
  ],
  "properties": {
    "artifact_type": {
      "const": "phase4_backtest_summary"
    },
    "game": {
      "enum": [
        "ssq",
        "dlt"
      ]
    },
    "selected_candidate_identity": {
      "pattern": "^[0-9a-f]{64}$"
    },
    "selection_indices": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "integer"
      }
    },
    "report_only_indices": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "integer"
      }
    },
    "model_selection_receipt_path": {
      "pattern": "^models/(ssq|dlt)/model-selection-receipt\\.json$"
    },
    "model_selection_receipt_sha256": {
      "pattern": "^[0-9a-f]{64}$"
    },
    "selection_receipt_hash": {
      "pattern": "^[0-9a-f]{64}$"
    },
    "overlap_count": {
      "const": 0
    },
    "comparator": {
      "const": "M0"
    },
    "report_only_summary": {
      "type": "object"
    },
    "metrics": {
      "type": "array",
      "minItems": 7,
      "uniqueItems": true
    },
    "scientific_status": {
      "enum": [
        "lift_supported",
        "no_confirmed_lift",
        "worse_than_M0",
        "insufficient_evidence"
      ]
    },
    "unfavorable_results_retained": {
      "const": true
    },
    "status": {
      "const": "PASS"
    }
  }
}
